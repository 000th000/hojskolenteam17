<div id="header" class="w-screen h-20 fixed overflow-hidden z-10 bg-transparent transition-all duration-300">
  <div class="flex w-full sm:px-4 md:px-16 justify-between text-white h-full">
    <!-- Left content -->
    <div class="flex items-center">
      <img id="logo" class="w-[157px] h-[41px] mr-10 opacity-0 transition-opacity duration-300 hidden-logo rounded-none" src="src/assets/images/hojskolelogo.png" />
      <nav id="left-nav" class="hidden md:flex space-x-10 transition-transform duration-300">
        <a href="">Rejser</a>
        <a href="">Mød os</a>
        <a href="">I tvivl?</a>
      </nav>
    </div>

    <!-- Right content -->
    <div class="flex items-center">
      <nav class="hidden md:flex space-x-10">
        <a class="place-self-center" href="">Blog</a>
        <a href=""><button class="px-4 py-2 bg-Orange-500 rounded-3xl text-white">Tilmed</button></a>
      </nav>

      <!-- Mobile Menu Button -->
      <button class="md:hidden text-white focus:outline-none">
        <svg class="h-8 w-8" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke="currentColor">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16m-7 6h7" />
        </svg>
      </button>
    </div>
  </div>
</div>


<style>
    .hidden-logo {
  opacity: 0;
  visibility: hidden;
}

.visible-logo {
  opacity: 1;
  visibility: visible;
}

.left-start {
  transform: translateX(-50%);
}

.left-original {
  transform: translateX(0);
}

.solid-bg {
  background-color: rgba(255, 156, 102, 1) ;
}

.transition-all {
  transition: all 0.3s ease-in-out;
}

</style>

<script>
  const header = document.getElementById('header');
  const logo = document.getElementById('logo');
  const leftNav = document.getElementById('left-nav');

  window.addEventListener('scroll', () => {
    if (window.scrollY > 50) { // Adjust scroll threshold as needed
      header.classList.add('solid-bg');
      logo.classList.remove('hidden-logo');
      logo.classList.add('visible-logo');
      leftNav.classList.remove('left-start');
      leftNav.classList.add('left-original');
    } else {
      header.classList.remove('solid-bg');
      logo.classList.remove('visible-logo');
      logo.classList.add('hidden-logo');
      leftNav.classList.remove('left-original');
      leftNav.classList.add('left-start');
    }
  });
</script>
